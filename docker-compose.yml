version: '3.8'

services:
  vectorcore:
    build: .
    container_name: vectorcore_app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8000
      - HOST=0.0.0.0
    restart: unless-stopped
